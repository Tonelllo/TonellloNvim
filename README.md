# What's this
This is my nvim configuration.
# How to install
To use this config you need neovim >= 0.10. To install it head over to neovim github and 
get the latest appimage.

After the installation is done you can copy this config inside
```~/.config/nvim/```
